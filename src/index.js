  import {speak} from './example.js';

  document.addEventListener("DOMContentLoaded", function(event) {
   speak();
});

let add = (a,b) => {
    return a+b;
 };
 let c = add(10, 20);
 console.log(c);
